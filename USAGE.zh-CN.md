# 中文使用说明

[English](USAGE.md)

这是一套连接品牌策略与门店经营的 Agent Skills，共有五个 Skill。你可以只调用当前需要的一个，也可以把五个串成完整流程。简单、已获授权的任务不必依次调用全部五个 Skill。

本仓库使用 [Apache License 2.0](LICENSE) 开源。

## 安装与更新

首次使用时，通过 Skills CLI 把全部五个 Skill 安装到它检测到的所有受支持 Agent 环境：

```bash
npx skills add clank123/blackshark-brand-store-skills --all -g
```

以后更新所有全局安装的 Skill（不只更新本包）：

```bash
npx skills update -g
```

安装或更新后，请新开一个 Agent 会话，让最新 Skill 被重新加载。仅仅 `git pull` 只会更新源码；通过上面的 Skills CLI 安装和更新，更适合同事直接使用。

## 一分钟选择：我该用哪个 Skill？

| 你现在要解决的问题 | 使用的 Skill | 你会得到什么 |
| --- | --- | --- |
| 理清品牌意图、门店角色和顾客场景之间的关系 | `blackshark-brand-store-strategy` | 一份带选择依据和证据边界的策略关系模型 |
| 判断一个结论还缺什么证据、下一步应该验证什么 | `blackshark-operating-validation` | 与决策风险相匹配的验证方案和证据约定 |
| 把已经确认的方向变成完整动作，同时看清依赖和优先级 | `blackshark-action-planning` | 完整动作结构，以及与其分开的优先级视图 |
| 把任务交给一个人或一个协作团队执行 | `blackshark-project-handoff` | 与责任和风险相匹配的交接稿 |
| 结果回来后，判断继续、调整、完成还是停止 | `blackshark-operating-review` | 不超出证据的复盘结论和后续决定 |

如果你只是要把一个已经确认、风险较低的小改动交给执行人，通常可以直接使用 `blackshark-project-handoff`，不需要先走完整流程。

## 开始前提供什么

只给当前问题真正需要的信息即可，不必先填一份庞大的表格。通常包括：

- 业务目标和眼下要回答的问题；
- 门店角色、顾客场景以及重要限制；
- 已确认的决定、负责人和当前授权；
- 哪些地方发生了变化、哪些没有变化，以及明确不做什么；
- 已有证据、缺失证据和你希望得到的输出。

可以参考 `examples/business-context.synthetic.json` 中的完全虚构示例，但它不是必填模板。

## 接续已有工作时，避免重复审批

请明确告诉 Agent 哪些决定已经确认、谁负责，以及当前授权覆盖到哪里。Skill 应继承没有变化的事实、负责人、授权和非目标。如果只有一个假设改变，就只重新打开受影响的部分，不要要求所有参与者重新批准整个方向。

Gate 是决定条件，不等于会议或审批。信息不足时，应缩小相关结论，或只阻塞依赖这条信息的动作，而不是让全部工作停下来。

## 新问题怎样走完整流程

当问题是新的，或原有问题发生了实质变化时，可以按下面的顺序使用：

1. 用 `blackshark-brand-store-strategy` 连接目标、门店角色、顾客旅程、产品或服务、渠道、体验和资源选择。
2. 用 `blackshark-operating-validation` 说明需要什么证据，以及现在还不能声称什么。
3. 方向确认后，用 `blackshark-action-planning` 保留完整依赖，再单独形成优先级视图。
4. 用 `blackshark-project-handoff` 根据真实责任、协作和风险，选择轻量、协作或复杂交接。
5. 结果回来后，用 `blackshark-operating-review` 分别检查执行、顾客反应、门店经营、品牌网络影响和方法可靠性。

完整的虚构演示见 `examples/end-to-end.synthetic.md`。它只用于说明流程，不代表真实经营结果。

## 工具、隐私和授权边界

这些 Skill 可以生成分析、计划、草稿和交接文本，但本身不会发送消息、发布内容、修改外部系统或替任何人作出承诺。即使宿主 Agent 可以调用工具，任何外部动作仍需具备该动作所要求的授权。

账号凭证、顾客记录、员工身份、内部链接、私有文档和真实经营数据都不应写入可复用的 Skill 文件。私有业务信息只在当前获授权的任务中提供，并遵守所在环境的数据保留和访问规则。

## 怎样判断结果

请把下面四件事分开，不要因为其中一项完成就推断其他项也成立：

- 准备度：工作是否已经可以开始；
- 执行情况：承诺的动作实际完成了什么；
- 经营效果：顾客行为或商业结果发生了什么变化；
- 方法可靠性：这套方法在什么条件下可以重复使用。

完成交接不代表已经产生经营效果。一次正向结果可以支持继续验证，但不能单独证明方法普遍有效。

## 可以直接复制的提问方式

开放的策略问题：

> 使用 `blackshark-brand-store-strategy`，为两家虚构门店共同推出一款产品设计不同的门店角色。请标出假设和证据边界。

已经批准的低风险改动：

> 方向、负责人和授权都已经确认。使用 `blackshark-project-handoff` 生成一份轻量文字修改交接，不要新建项目，也不要重新请求没有变化的审批。

证据有限的复盘：

> 使用 `blackshark-operating-review`，比较预期顾客行为和目前观察到的证据。没有测量到的经营效果继续标记为未知。
